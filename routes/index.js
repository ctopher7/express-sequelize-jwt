var express = require('express');
var router = express.Router();
const dayjs = require('dayjs')
// router.get('/', function(req, res, next) {
//   res.send({ title: dayjs().set('date',5).set('month',5).set('year',2015) });
// });

module.exports = router;
